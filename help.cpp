//
//  help.cpp
//  FEDS
//
//  Created by rujan on 20/07/2024.
//

#include "help.hpp"
#include<iostream>
using namespace std;
/*
int main()
{
    int i=0,depth,count=0,vertCount=0,nextPosition;
    string word,newword;
    word="aakashthakur";
    depth=4;
    
    switch(depth)
        case 3:
            nextPosition=4;
    
    nextPosition= 2*(depth-1);
            
    cout<<nextPosition;
    
    while(word[i])
    {
        cout<<endl<<i<<word[i]<<endl;
        newword.append(1,word[i]);
    count++;
    i+=nextPosition;
            
    if(!word[i]){
        cout<<"test";
        vertCount++;
        
        if (count<word.length())
        
            {
                cout<<"test2;";
            i=vertCount;
    
    
            }
        if(count>word.length())
        {break;
        }
        
        if(vertCount+1<depth)
            {nextPosition-=2;
            }
        if((vertCount+1)==depth)
        {
        nextPosition=2*(depth-1);
            }
}
    
    }
    
            cout<<endl<<word<<endl<<"atahhrksauak"<<endl<<newword<<endl;
    return 0;
}
*/
